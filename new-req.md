Use Retrieval augmented generation (RAG)
to search for relevant skills to execute
Let the model used for RAG be configurable, default to `ollama`'s RAG offering

Add the following default skills to test:

Add a `clock` skill that fetches date and time
Add a `weather` skill that fetches weather data
